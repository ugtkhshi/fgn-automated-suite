# fgn-automated-suite

Pytest + Playwright sample automation project.

Quick start:

```powershell
python -m venv venv
. .\venv\Scripts\Activate.ps1
pip install -r requirements.txt
python -m playwright install
pytest -q
```

TypeScript Playwright quick start:

```powershell
npm install
npx playwright install
npx playwright test
```
